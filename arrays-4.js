const array = ['html', 'python', 'java', 'javascript', 'CSS', 'C++', 'C#', 'PHP', 'pinescript', 'kotlin']

let i = 0;

while(i < array.length) {
    if (array[i].length > 3) {
        console.log(array[i])
        
    }
    i++
}

